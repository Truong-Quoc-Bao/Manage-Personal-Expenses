const path = require('path');

module.exports = {
    ACCOUNT_PROTO_PATH:   path.join(__dirname, 'account.proto'),
    CATEGORY_PROTO_PATH:  path.join(__dirname, 'category.proto'),
    BUDGET_PROTO_PATH:    path.join(__dirname, 'budget.proto'),
    USER_PROTO_PATH:      path.join(__dirname, 'user.proto'),
    ANALYTICS_PROTO_PATH: path.join(__dirname, 'analytics.proto'),
};